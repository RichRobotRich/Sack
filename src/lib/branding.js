/**
 * Zentrale Stelle für Marken-Assets.
 *
 * Das Logo liegt unter public/ und wird damit mit der App ausgeliefert – es
 * hängt an keinem fremden Dienst mehr. Erzeugt wird es zusammen mit den
 * Symbolen aus assets/logo-source.png, siehe scripts/generate-icons.mjs.
 */
export const LOGO_URL = '/logo.png';

export const APP_NAME = 'Leniger';
